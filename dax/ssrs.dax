= IIF(
    LEN(
        CSTR(MONTH(TODAY()) - 1)
    ) = 1,
    "0" + CSTR(MONTH(TODAY()) - 1),
    CSTR(MONTH(TODAY()) - 1)
)



IIF(
    LEN(
        CSTR(MONTH(TODAY()) - 2)
    ) = 1,
    "0" + CSTR(MONTH(TODAY()) - 2),
    CSTR(MONTH(TODAY()) - 2)
)